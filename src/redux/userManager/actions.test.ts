/*import { ActionCreatorNew } from "./actions.new";

const { logIn } = ActionCreatorNew({
  baseUrl: 'mocked.api',
  json: { stringify: jest }
})*/

it('test ttypescript', () => {
  expect(true).toBe(true);
});